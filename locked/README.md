# LOCKED VERSION — DO NOT MODIFY

This folder contains the working demo version (v1.0-demo-working).

**These files must never be edited.** All future development happens in `/bold/`, `/final.html`, etc.

## Live URLs (permanent, never change)

- https://comfy-donut-f8587d.netlify.app/locked/
- https://comfy-donut-f8587d.netlify.app/locked/app

## Restore from backup

If `/bold/app.html` ever breaks, restore with:

```
cp locked/app.html bold/app.html
git add bold/app.html && git commit -m "Restore from locked v1.0"
git push
```

## Git tag

`v1.0-demo-working` — commit `450da7e` (chart destroy fix)
